name: Build all Typst documents

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # 1. Checkout
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. Trouver tous les fichiers .typ
      - name: Find Typst files
        id: find
        shell: bash
        run: |
          echo "🔍 Searching for .typ files..."
          mapfile -d '' -t files < <(find . -type f -name '*.typ' ! -path './config.typ' -print0)
          if [ ${#files[@]} -eq 0 ]; then
            echo "❌ No Typst files found. Exiting."
            exit 1
          fi
          # Écrire les fichiers dans un fichier temporaire pour Typst
          printf '%s\n' "${files[@]}" > typst_files.txt
          # Exporter la liste au format multi-line string pour Typst
          TYPST_FILES=$(cat typst_files.txt | sed 's/^/  /')
          echo "TYPST_FILES<<EOF" >> $GITHUB_ENV
          cat typst_files.txt >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV

      # 3. Compiler tous les fichiers en une seule invocation Typst
      - name: Typst
        uses: lvignoli/typst-action@main
        with:
          source_file: ${{ steps.find.outputs.TYPST_FILES }}
          options: |
            --root
            .

      # 4. Renommer tous les PDFs avec timestamp
      - name: Move PDFs to release with timestamp
        shell: bash
        run: |
          mkdir -p release
          TIMESTAMP=$(date +%Y-%m-%d-%H%M%S)
          while read -r typfile; do
            base=$(basename "$typfile" .typ)
            pdf="${typfile%.typ}.pdf"
            if [ -f "$pdf" ]; then
              mv "$pdf" "release/${base}_${TIMESTAMP}.pdf"
            fi
          done < typst_files.txt
          echo "All PDFs moved to release/ with timestamp."

      # 5. Upload bundle final des PDFs
      - name: Set artifact name
        id: artifact
        run: |
          TIMESTAMP=$(date +%Y-%m-%d-%H%M%S)
          ARTIFACT_NAME="PDFs_${TIMESTAMP}"
          echo "ARTIFACT_NAME=$ARTIFACT_NAME" >> $GITHUB_ENV
          echo "Artifact will be named: $ARTIFACT_NAME.zip"

      - name: Upload PDFs artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.artifact.outputs.ARTIFACT_NAME }}
          path: release/*.pdf

      # 6. Créer Release (si push avec un tag)
      - name: Create Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          name: "Cours — ${{ github.ref_name }}"
          files: release/*.pdf
